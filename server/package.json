{
  "name": "server",
  "version": "1.0.0",
  "description": "A node server for multi-user rooms and local development.",
  "type": "module",
  "scripts": {
    "servers": "concurrently npm:server:*",
    "server:socket": "node socket.js",
    "server:api": "node api.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeffrey Alan Scudder",
  "license": "ISC",
  "dependencies": {
    "chokidar": "^3.5.3",
    "concurrently": "^7.2.2",
    "dotenv": "^16.0.0",
    "fastify": "^4.2.0",
    "ip": "github:jaiswarvipin/node-ip",
    "ws": "^8.4.0"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.6",
    "utf-8-validate": "^5.0.8"
  },
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
